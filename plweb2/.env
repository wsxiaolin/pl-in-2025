VITE_API_URL=/api
VITE_STATIC_URL=/static
VITE_ROOT_URL="/#"
VITE_BASE_URL=""
VITE_GITHUB_DATA_URL=/github-data

# dev时候API请求的路径，本地会代理/api和/static的请求
# The path where API requests are sent during dev, locally /api and /static requests are proxied
# @see vite.config.ts